---
- name: 로컬 로그 보존 기준 적용
  hosts: linux_servers
  gather_facts: true
  become: true
  serial: 5
  max_fail_percentage: 0

  tasks:
    - name: 로컬 로그 보존 Role 실행
      ansible.builtin.import_role:
        name: logging_baseline
        tasks_from: local_logging
