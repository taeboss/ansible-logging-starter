---
- name: 중앙 로그 전송 기준 적용
  hosts: linux_servers
  gather_facts: true
  become: true
  serial: 5
  max_fail_percentage: 0

  tasks:
    - name: 중앙 로그 전송 Role 실행
      ansible.builtin.import_role:
        name: logging_baseline
        tasks_from: central_logging
