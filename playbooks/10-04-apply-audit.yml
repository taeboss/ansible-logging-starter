---
- name: audit 감사 규칙 적용
  hosts: linux_servers
  gather_facts: true
  become: true
  serial: 5
  max_fail_percentage: 0

  tasks:
    - name: audit 감사 규칙 Role 실행
      ansible.builtin.import_role:
        name: logging_baseline
        tasks_from: audit
