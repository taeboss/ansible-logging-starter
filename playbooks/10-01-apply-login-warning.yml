---
- name: 로그인 전 경고 및 시스템 정보 비노출 적용
  hosts: linux_servers
  gather_facts: true
  become: true
  serial: 5
  max_fail_percentage: 0

  tasks:
    - name: 로그인 전 경고 및 정보 비노출 Role 실행
      ansible.builtin.import_role:
        name: logging_baseline
        tasks_from: login_warning
