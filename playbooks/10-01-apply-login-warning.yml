---
- name: 콘솔 호스트명 비노출 및 SSH 상세 인증 로그 적용
  hosts: linux_servers
  gather_facts: true
  become: true
  serial: 5
  max_fail_percentage: 0

  tasks:
    - name: 콘솔 비노출 및 SSH 인증 로그 Role 실행
      ansible.builtin.import_role:
        name: logging_baseline
        tasks_from: login_warning
