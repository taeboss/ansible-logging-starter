---
- name: NTP 시간 동기화 기준 적용
  hosts: linux_servers
  gather_facts: true
  become: true
  serial: 5
  max_fail_percentage: 0

  tasks:
    - name: NTP 시간 동기화 Role 실행
      ansible.builtin.import_role:
        name: logging_baseline
        tasks_from: ntp
